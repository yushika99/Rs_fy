
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".IncomeGoalActivity"
    android:orientation="vertical"
    android:background="@drawable/incomeBackground1">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/my_income_goals"
        android:textColor="#000000"
        android:textAlignment="center"
        android:textSize="25dp"
        android:textStyle="bold"
        android:gravity="center_horizontal" />

    <TextView
        android:id="@+id/TotalIncomeGoalTextview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:textSize="18dp"
        android:background="@drawable/headtextsub"
        android:gravity="center"
        android:text="Total Income Goal : Rs. 0.00 "
        android:textColor="#FFF"
        android:textStyle="bold" />

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/goaltoolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:elevation="1dp"
        android:theme="@style/AppTheme"
        app:popupTheme="@style/AppTheme" />

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/goalrecyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/goalfab"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center"
            android:layout_margin="10dp"
            android:backgroundTint="#ff1a1a"
            android:scaleType="centerCrop"
            android:src="@drawable/incomeplusred"
            app:fabCustomSize="10mm" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>



</LinearLayout>